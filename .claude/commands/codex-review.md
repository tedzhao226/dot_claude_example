Use the codex-review agent to review code in the current workspace.

Pass through any scope the user specified (uncommitted changes, branch name, commit SHA).
If no scope is given, default to uncommitted changes.

$ARGUMENTS
